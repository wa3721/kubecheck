# e2e C1：正常通过场景（阶段1 滚动更新 + 阶段2 Running + 阶段3 日志无错误）-> 期望 exit 0
# 对应 e2e/e2e-test.sh run_c1
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-c1
  namespace: flowtest
  labels:
    app: flow-c1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-c1
  template:
    metadata:
      labels:
        app: flow-c1
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
