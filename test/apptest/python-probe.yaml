apiVersion: v1
kind: Namespace
metadata:
  name: app-python
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-probe
  namespace: app-python
  labels:
    app: python-probe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-probe
  template:
    metadata:
      labels:
        app: python-probe
    spec:
      containers:
        - name: python-probe
          image: demo-python:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          # startup：慢启动保护，启动成功后自动停止探测
          startupProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 1
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 30
            successThreshold: 1
          # readiness：就绪判断，成功后加入 Service 端点
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          # liveness：存活判断，比 readiness 更宽松避免抖动重启
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
            successThreshold: 1
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 250m
              memory: 128Mi
